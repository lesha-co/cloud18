# Reddit Crawler Configuration

# Firefox cookies file path (required for Reddit authentication)
# Path to your Firefox profile's cookies.sqlite file
# Example: /Users/username/Library/Application Support/Firefox/Profiles/abc123.default/cookies.sqlite
COOKIES_FILE=/path/to/your/firefox/profile/cookies.sqlite

# Database file path for storing the crawled subreddit graph
# This is used by both the crawler and analysis scripts
# Example: graph.db
DATABASE_FILE=graph.db

# Delay in milliseconds between page visits to avoid rate limiting
# Recommended: 2000 (2 seconds)
DELAY=2000

# Browser headless mode (true/false)
# Set to true for background crawling, false to see the browser
HEADLESS=true

# Reddit username (optional)
# If provided, will seed the crawler from the user's multireddits
USERNAME=

# Reddit password (unused in current version - cookie auth is used instead)
PASSWORD=

# Output files for cluster analysis
OUTPUT_FILE=clusters.json
VIZ_OUTPUT_FILE=subreddit-hubs.html
